dont expand in heredoc if eof contains quotes anywhere
if dollar followed by quote, $ expand to nothing (is removed)
if command not found, specific $?
if directory, say is directory and return specific $?
cat < inf | ls: unitialized bytes in close and dup2
